# Optional: must match backend listen port (see backend terminal output).
# On macOS, port 5000 is often taken by AirPlay — use PORT=5001 in backend/.env, then:
# REACT_APP_PROXY_TARGET=http://localhost:5001
