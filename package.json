{
  "name": "pawpact",
  "private": true,
  "description": "Run API + React together from the PawPact folder",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
